# 얼굴 인식 이해 가이드

이 프로젝트의 얼굴 인식 동작 원리와 해당 구현 위치를 정리한 문서다.
컴퓨터 비전 사전 지식이 없어도 읽을 수 있도록 개념부터 순서대로 기술한다.

---

## 0. 한 줄 요약

> 카메라 화면에서 **얼굴 위치를 찾고**(검출), 그 얼굴이 **등록된 사람 중 누구인지 판별한다**(인식).
> 두 가지는 서로 다른 기술이며 반드시 이 순서로 동작한다.

```
카메라 프레임 → [검출] 얼굴 위치 → [잘라내기·크기 통일] → [인식] 누구인가
```

---

## 1. 이미지는 숫자 배열이다

카메라에서 받은 화면 한 장(프레임)은 `cv::Mat` 타입이며, 실체는 **숫자 배열**이다.

640×480 컬러 이미지의 경우 다음과 같다.

```
가로 640칸 × 세로 480칸 × 색 3개(B, G, R)
= 921,600개의 숫자 (각각 0~255)
```

두 가지를 전제로 한다.

**첫째, OpenCV는 RGB가 아닌 BGR 순서를 사용한다.** Qt 화면에 표시할 때 `cvtColor(BGR2RGB)` 로
변환하는 이유이며, 누락하면 화면이 파랗게 출력된다.

**둘째, 얼굴 검출은 색상을 사용하지 않는다.** 명암만 사용하므로 흑백으로 변환해 처리한다.
데이터가 1/3로 줄어 처리 속도도 향상된다.

---

## 2. 검출과 인식은 다른 문제다

두 개념의 구분이 이 문서에서 가장 중요하다.

| | 검출 (Detection) | 인식 (Recognition) |
| --- | --- | --- |
| 질문 | "얼굴이 어디 있나?" | "이 얼굴이 누구인가?" |
| 출력 | 사각형 좌표 `(x, y, w, h)` | 운전자 ID + 유사도 |
| 사용 도구 | Haar Cascade | LBPH |
| 등록 데이터 | 필요 없음 | 반드시 필요 |

**검출은 대상을 가리지 않는다.** 등록 여부와 무관하게 "사람 얼굴 형태의 영역"을 모두 찾는다.

**인식은 등록된 데이터와의 비교가 전제된다.** 등록 데이터가 없으면 동작하지 않는다.
본 프로그램에서 얼굴 등록을 먼저 수행해야 인식이 가능한 이유다.

---

## 3. 얼굴 검출 — Haar Cascade

### 원리: 명암 대비 규칙

사람 얼굴에는 일반적으로 성립하는 명암 패턴이 존재한다.

- 눈 부위는 볼보다 어둡다
- 콧등은 양옆보다 밝다
- 눈썹 라인은 이마보다 어둡다

이러한 규칙을 **흑백 사각형 쌍**으로 표현한다. 한 영역의 밝기 합에서 다른 영역의 밝기 합을 뺀 값이
크면 얼굴일 가능성이 높다고 판정하는 방식이다.

```
■■■■  ← 어두운 영역 (눈)
□□□□  ← 밝은 영역 (볼)
```

본 프로젝트에서 사용하는 `haarcascade_frontalface_default.xml` 에는 이러한 규칙이 **2,913개** 포함되어 있으며
25단계로 구성된다. 얼굴 사진 수천 장으로 **사전 학습된 결과물**이며, 프로그램은 이 파일을 읽어 사용한다.

### 탐색 방식

작은 사각형 창을 이미지 위에서 한 칸씩 이동시키며 내부가 얼굴인지 규칙에 통과시킨다.
전체를 한 번 순회하면 창 크기를 10% 확대해 다시 순회한다. 근거리와 원거리 얼굴을 모두 검출하기 위해서다.

640×480 이미지 한 장에서 **약 300만 개의 창**을 검사한다. 그럼에도 실시간 처리가 가능한 이유는
**단계별 탈락 구조(cascade)** 에 있다.

```
1단계 (규칙 9개)   → 탈락하면 즉시 버림
2단계 (규칙 16개)  → 탈락하면 즉시 버림
3단계 (규칙 27개)
...
25단계             → 전부 통과해야 "얼굴"
```

배경과 같이 명백한 비얼굴 영역은 1단계에서 탈락한다. 이미지 대부분이 배경이므로
실제로는 규칙 9개만 계산하고 종료되는 창이 대다수다.
전체를 계산할 경우 약 88억 회가 필요한 연산이 약 1억 회로 감소한다.

### 조정 가능한 파라미터

`faceengine.cpp` 의 `detect()` 함수에 위치한다.

```cpp
m_detector.detectMultiScale(small, faces, 1.1, 5, 0, cv::Size(minSide, minSide));
//                                        ↑    ↑              ↑
//                                   scaleFactor  minNeighbors  최소 얼굴 크기
```

| 값 | 의미 | 올리면 | 내리면 |
| --- | --- | --- | --- |
| `1.1` | 창을 키우는 비율 | 빨라짐, 놓칠 수 있음 | 촘촘함, 느려짐 |
| `5` | 같은 자리에서 몇 번 겹쳐 검출돼야 인정 | 확실한 것만, 놓침 증가 | 잘 잡힘, 오검출 증가 |
| `minSide` | 이보다 작은 얼굴은 무시 | 먼 얼굴 무시 | 작은 오검출 증가 |

---

## 4. 정규화

검출 결과는 **좌표 정보에 불과하며**, 그대로 인식에 사용할 수 없다.

대상과 카메라의 거리에 따라 얼굴 크기가 달라지고 배경이 포함되기 때문이다.
따라서 다음 세 가지 처리를 수행한다. (`faceengine.cpp` 의 `cropFace()`)

```cpp
cv::Mat face;
cv::resize(gray(safe), face, cv::Size(200, 200));  // ① 크기 통일
cv::equalizeHist(face, face);                      // ② 밝기 보정
```

**① 얼굴 영역만 추출한다.** 배경이 포함되면 배경까지 해당 인물의 특징으로 학습된다.

**② 200×200으로 크기를 통일한다.** 크기가 다르면 비교가 성립하지 않는다.

**③ `equalizeHist` 로 밝기 분포를 평활화한다.** 저조도 환경과 고조도 환경에서 촬영된 얼굴을
유사한 분포로 맞춰 조명 차이의 영향을 줄인다.

> 이 정규화 단계가 인식 정확도를 크게 좌우한다. 여기가 부정확하면 이후 단계의 성능도 확보되지 않는다.

---

## 5. 얼굴 인식 — LBPH

**L**ocal **B**inary **P**attern **H**istogram(국소 이진 패턴 히스토그램)의 약자이며,
명칭 그대로 세 단계로 구성된다.

### ① 국소 이진 패턴 (LBP)

각 픽셀을 주변 8개 픽셀과 비교한다. **기준 픽셀보다 밝으면 1, 어두우면 0** 을 부여한다.

```
주변 밝기          비교 결과        8자리 이진수
120  135  140      0    1    1
 90  [100] 115  →  0   [ ]  1    →  01101011 = 107
 85   95  110      0    0    1
```

이 과정을 거치면 모든 픽셀이 0~255 범위의 값 하나를 갖는다.

**절대 밝기가 아니라 이웃 픽셀과의 대소 관계만 사용한다는 점이 핵심이다.**
조명이 전체적으로 어두워져도 대소 관계는 유지되므로 조명 변화에 강한 특성을 갖는다.

### ② 히스토그램

히스토그램은 **각 값이 몇 번 나타났는지 집계한 표**다.

시험 점수 30개를 10점 단위로 집계하면 `60점대 3명, 70점대 8명, 80점대 12명` 형태가 되며 이것이 히스토그램이다.
개별 값은 사라지고 전체 분포만 남는다.

LBPH는 이미지를 **격자(8×8 = 64칸)로 분할해 칸마다 개별적으로** LBP 값을 집계한다.

```
64칸 × 256가지 값 = 16,384개의 숫자
```

**격자로 분할하는 이유**는 전체를 한 번에 집계하면 위치 정보가 소실되기 때문이다.
칸별로 집계하면 눈에 해당하는 칸과 코에 해당하는 칸의 패턴 분포가 각각 보존된다.
이 16,384개의 수치가 해당 얼굴의 **특징 벡터**에 해당한다.

### ③ 거리 비교

인식 시에는 입력 얼굴의 특징 벡터를 등록된 벡터들과 비교해 **거리가 가장 가까운 인물**을 선택한다.
이 거리 값이 코드상의 `confidence` 이자 화면에 표시되는 **score** 다.

> **score는 값이 작을수록 유사도가 높다.** 변수명이 confidence(신뢰도)이지만
> 실제 의미는 거리(distance)이므로, 0에 가까울수록 동일 인물일 가능성이 크다.

---

## 6. 임계값 설정

LBPH는 **항상 거리가 가장 가까운 인물을 반환한다.** 미등록자가 입력되어도 등록된 인물 중
가장 유사한 대상을 결과로 출력한다. 따라서 판별 기준선이 필요하다.

`faceengine.h`

```cpp
static constexpr double kThreshold = 70.0;   // 이보다 크면 미등록으로 판단
```

```
score  0 ─────── 40 ─────── 70 ─────── 120 ───→
       │  본인    │  애매   │  남/미등록
                          ↑ 임계값
```

### 실측 절차

이 값은 조명, 카메라, 등록 사진 품질에 따라 달라지므로 **실측을 통해 결정해야 한다.**

1. 팀원 전원을 등록한다
2. 각자 인식을 실행해 본인의 score 를 기록한다 (일반적으로 20~50)
3. 미등록자의 score 를 기록한다 (일반적으로 80 이상)
4. 두 구간 사이에 임계값을 설정한다

등록자가 거부되면 값을 올리고, 미등록자가 통과하면 값을 내린다.
등록 사진의 조명과 각도가 다양할수록 두 구간의 간격이 벌어져 기준 설정이 용이하다.

### 5프레임 연속 조건

단일 프레임만으로 판단하면 순간적인 오인식으로 잘못된 인증이 발생할 수 있다.
따라서 **동일 인물이 5회 연속 매칭될 때** 인증을 확정한다. (`recognitionthread.h` 의 `kMatchStreak`)

화면에 표시되는 `홍길동 (score 42, 3/5)` 에서 `3/5` 가 이 카운트다.

---

## 7. 코드 구조와 구현 위치

```
카메라 ─→ CaptureThread ─→ RecognitionThread ─→ FaceEngine
                                    │                ├─ 검출 (Haar)
                                    │                └─ 인식 (LBPH)
                                    ↓
                              MainWindow ─→ DatabaseManager ─→ SQLite
```

| 파일 | 역할 | 조정 대상 |
| --- | --- | --- |
| `capturethread.cpp` | 카메라에서 최신 프레임만 계속 읽음 | 카메라 인덱스, 해상도, 백엔드 |
| `faceengine.cpp` | 검출 + 정규화 + 학습 + 인식 | 검출 파라미터, 얼굴 크기, 임계값 |
| `recognitionthread.cpp` | 프레임을 받아 검출/인식을 반복, 결과를 시그널로 전달 | 검출 주기, 샘플 수집 간격, 연속 매칭 횟수 |
| `mainwindow.cpp` | 버튼 연결, 화면 표시, DB 저장, 화면 전환 | 등록 샘플 수, 화면 흐름 |
| `databasemanager.cpp` | 운전자·샘플·인증 이력 저장과 조회 | 스키마, 쿼리 |

### 스레드 분리 이유

카메라 읽기는 지연이 발생하는 작업이다. UI 스레드에서 수행하면 **화면이 멈춘다.**
따라서 캡처 전용 스레드가 별도로 동작하며 최신 프레임 한 장만 유지하고,
표시와 인식은 이를 참조한다.

**카메라 한 대당 `CaptureThread` 는 하나만 생성한다.**
동일 카메라를 두 번 열면 Windows(DirectShow)에서 두 번째 시도가 실패한다.
이 때문에 영상 표시와 얼굴 인식이 동일한 캡처 스레드를 공유한다.

### 인식 스레드가 DB에 직접 접근하지 않는 이유

`QSqlDatabase` 커넥션은 **생성한 스레드에서만** 사용할 수 있다.
따라서 인식 스레드는 검출한 얼굴 이미지를 시그널로 UI 스레드에 전달하고, 저장은 `MainWindow` 가 수행한다.

이 과정에서 `cv::Mat` 을 시그널 인자로 사용하므로 `main.cpp` 에 다음 등록이 필요하다.

```cpp
qRegisterMetaType<cv::Mat>("cv::Mat");
```

누락 시 시그널이 **경고 없이 폐기되어**, 등록이 정상 완료된 것처럼 보이지만 샘플이 저장되지 않는다.

---

## 8. 데이터 흐름

### 등록 과정

```
[얼굴 등록] 클릭
   → driver 테이블에 이름 INSERT (id 획득)
   → 매 프레임 얼굴 검출 → 200×200으로 잘라냄
   → 검출 3회마다 1장씩 PNG 저장 + face_sample 에 경로 INSERT
   → 20장 모이면 자동 재학습 → lbph_model.yml 저장
```

### 인증 과정

```
[인식 시작] 클릭 → FACE_DETECTION_START 이벤트 저장
   → 매 프레임 얼굴 검출 → 200×200으로 잘라냄
   → LBPH로 가장 가까운 사람 + score 계산
   → score ≤ 70 이고 같은 사람 5회 연속 → 인증 확정
   → auth_log INSERT + FACE_DETECTED 이벤트 저장
   → 잠금 해제 + 계기판 화면으로 전환
```

---

## 9. 증상별 원인 진단

| 증상 | 원인 |
| --- | --- |
| 초록 사각형이 표시되지 않음 | **검출 실패.** 저조도, 얼굴 크기 부족, 비정면 각도 |
| 등록 진행률이 0에서 증가하지 않음 | 검출 실패 또는 카메라 열기 실패 |
| 등록자가 계속 미등록으로 판정됨 | **임계값 문제.** score 를 확인해 `kThreshold` 를 상향 |
| 미등록자가 인증을 통과함 | 임계값 하향. 등록 샘플 품질이 낮은 경우에도 발생 |
| score 가 항상 유사한 값으로 출력됨 | 등록 샘플의 각도·조명이 모두 동일할 가능성 |
| "등록된 얼굴이 없습니다" 표시 | 샘플 2장 미만. 운전자 조회에서 샘플 수 확인 |
| 초록 박스 위 문자가 `???` 로 표시 | `cv::putText` 는 한글을 지원하지 않는다. 정상 동작이며 이름은 하단 라벨에 표시된다 |

### 검출 실패 시 확인 순서

1. 카메라 영상이 출력되는가 → 미출력 시 카메라 문제이며 인식과 무관
2. 얼굴 크기가 충분한가 → 640×480 기준 얼굴 폭 80px 이상 필요
3. 정면을 향하고 있는가 → **본 프로젝트의 cascade 는 정면 전용**이며 30도 이상 회전 시 검출 불가
4. 조명이 얼굴 정면에서 오는가 → 역광 조건에서는 검출 실패

---

## 10. 파라미터 실험

값을 변경하며 결과를 관찰하면 각 파라미터의 성격을 파악할 수 있다.

**① `minNeighbors` 를 2와 10으로 변경** (`faceengine.cpp`)
2에서는 배경을 얼굴로 오검출하는 사례가 발생하고, 10에서는 검출률이 크게 떨어진다.

**② 임계값을 200으로 상향** (`faceengine.h`)
미등록자도 등록된 인물로 판정된다. LBPH 가 항상 가장 가까운 인물을 반환하는 성질을 확인할 수 있다.

**③ 저장된 얼굴 샘플 확인**
`faces/<driver_id>/` 폴더의 PNG 파일이 실제 학습에 사용되는 이미지다.
흐릿하거나 얼굴이 잘린 샘플이 포함되어 있으면 정확도가 저하되므로 정규화의 중요성을 확인할 수 있다.

**④ score 측정 기록**
등록자, 팀원, 미등록자의 score 를 표로 정리하면 임계값 설정 근거가 된다.

---

## 11. 자주 나오는 질문

**Q. 딥러닝 기반인가?**

아니다. LBPH 는 1990년대, Haar Cascade 는 2001년에 제안된 기법이다.
특징 추출 규칙(이웃보다 밝으면 1)을 **사람이 직접 설계한** 고전적 컴퓨터 비전 방식이다.
딥러닝은 이 규칙 자체를 데이터로부터 학습한다는 점에서 다르다.

따라서 "딥러닝 기반" 은 부정확한 표현이며, **"LBP 특징 기반 얼굴 인식"** 이 정확하다.

**Q. LBPH 의 학습이란 무엇인가?**

등록된 사진마다 히스토그램을 계산해 저장하는 것이 전부이며, 반복적인 가중치 최적화 과정이 없다.
학습보다는 **저장에 가까운 동작**이므로 즉시 완료되며 GPU 도 필요하지 않다.

**Q. 각도 변화에 취약한 이유는?**

얼굴을 눈·코·입의 배치 구조로 해석하지 않고 칸별 명암 패턴 통계로만 처리하기 때문이다.
얼굴이 회전하면 패턴이 다른 칸으로 이동해 전혀 다른 특징 벡터가 된다.
반면 조명 변화에는 강한데, 절대 밝기가 아닌 이웃 픽셀과의 대소 관계만 사용하기 때문이다.

**Q. 얼굴 사진이 로컬에 저장되는 문제는 없는가?**

LBPH 는 재학습 시 원본 이미지가 필요하므로 `faces/` 폴더에 얼굴 사진이 계속 보관된다.
이를 제거하려면 **임베딩(벡터) 방식**으로 전환해야 한다. 얼굴을 128차원 벡터로 변환해 DB 에 저장하고
원본은 즉시 삭제하는 방식이며, 벡터에서 얼굴을 복원하는 것은 사실상 불가능하다.

OpenCV 의 `FaceRecognizerSF`(SFace)로 구현할 수 있으나 ONNX 모델 파일(약 37MB)을 함께 배포해야 한다.
각도·조명 변화에 대한 내성은 더 우수하다. 현재 규모(4명, 실내 고정 환경)에서는 LBPH 로 충분하다고 판단해
현재 방식을 채택했다.

**Q. 샘플을 20장 등록하는 이유는?**

한 장만 등록하면 촬영 시점의 조명·각도·표정에만 최적화된다.
조건을 조금씩 달리한 20장을 수집하면 해당 인물의 변동 범위가 함께 반영된다.
등록 시 고개 방향과 표정을 다양하게 하도록 안내하는 이유다.

---

## 12. 용어 정리

| 용어 | 뜻 |
| --- | --- |
| 프레임 | 카메라에서 받은 이미지 한 장 |
| 검출 (Detection) | 얼굴의 **위치**를 찾는 처리 |
| 인식 (Recognition) | 해당 얼굴이 **누구인지** 판별하는 처리 |
| Haar Cascade | 명암 대비 규칙을 단계별로 적용하는 검출 기법 |
| LBPH | 국소 명암 패턴의 분포로 사람을 구별하는 인식 기법 |
| 히스토그램 | 값이 몇 개씩 나왔는지 세어놓은 표 |
| 정규화 | 얼굴을 동일한 크기·밝기로 맞추는 전처리 |
| score (confidence) | 등록된 얼굴과의 거리. **값이 작을수록 유사** |
| 임계값 (threshold) | 이 값을 넘으면 미등록으로 판단하는 기준선 |
| 캐스케이드 | 단계별 탈락을 통해 처리 속도를 확보하는 구조 |
