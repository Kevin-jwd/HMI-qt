# 얼굴 인식 이해 가이드

이 프로젝트의 얼굴 인식이 **어떤 원리로 동작하는지**, 그리고 **코드 어디를 보면 되는지** 정리한 문서입니다.
컴퓨터 비전을 처음 접해도 따라올 수 있도록 순서대로 썼습니다.

---

## 0. 한 줄 요약

> 카메라 화면에서 **얼굴 위치를 찾고**(검출), 그 얼굴이 **등록된 사람 중 누구인지 맞힙니다**(인식).
> 두 가지는 완전히 다른 기술이고, 반드시 이 순서로 동작합니다.

```
카메라 프레임 → [검출] 얼굴 위치 → [잘라내기·크기 통일] → [인식] 누구인가
```

---

## 1. 이미지는 그냥 숫자 배열이다

카메라에서 받은 한 장의 화면(프레임)은 `cv::Mat` 이라는 타입인데, 실체는 **숫자가 잔뜩 들어있는 표**입니다.

640×480 컬러 이미지라면 이렇습니다.

```
가로 640칸 × 세로 480칸 × 색 3개(B, G, R)
= 921,600개의 숫자 (각각 0~255)
```

여기서 두 가지만 기억하면 됩니다.

**첫째, OpenCV는 RGB가 아니라 BGR 순서입니다.** 파랑-초록-빨강 순으로 저장합니다.
Qt 화면에 띄울 때 `cvtColor(BGR2RGB)` 로 바꾸는 이유가 이것이고, 이걸 빼먹으면 화면이 파랗게 나옵니다.

**둘째, 얼굴 검출은 색을 안 봅니다.** 명암(밝기)만 보기 때문에 흑백으로 바꿔서 처리합니다.
데이터가 1/3로 줄어 속도도 빨라집니다.

---

## 2. 검출과 인식은 다른 문제다

가장 헷갈리기 쉬운 부분입니다.

| | 검출 (Detection) | 인식 (Recognition) |
| --- | --- | --- |
| 질문 | "얼굴이 어디 있나?" | "이 얼굴이 누구인가?" |
| 출력 | 사각형 좌표 `(x, y, w, h)` | 운전자 ID + 유사도 |
| 사용 도구 | Haar Cascade | LBPH |
| 등록 데이터 | 필요 없음 | 반드시 필요 |

**검출은 누구 얼굴이든 다 찾습니다.** 처음 보는 사람도 찾습니다. 그냥 "사람 얼굴처럼 생긴 영역"을 찾을 뿐입니다.

**인식은 미리 등록된 사람과 비교합니다.** 등록이 없으면 아무것도 못 합니다.
그래서 우리 프로그램도 얼굴 등록을 먼저 해야 인식 시작이 동작합니다.

---

## 3. 검출은 어떻게 하나 — Haar Cascade

### 아이디어: 명암 대비 규칙

사람 얼굴에는 항상 성립하는 명암 패턴이 있습니다.

- 눈 부위는 볼보다 어둡다
- 콧등은 양옆보다 밝다
- 눈썹 라인은 이마보다 어둡다

이런 규칙을 **흑백 사각형 쌍**으로 표현합니다. "이 영역의 밝기 합에서 저 영역의 밝기 합을 뺀 값이
크면 얼굴일 가능성이 있다" 는 식입니다.

```
■■■■  ← 어두운 영역 (눈)
□□□□  ← 밝은 영역 (볼)
```

우리가 쓰는 `haarcascade_frontalface_default.xml` 파일에는 이런 규칙이 **2,913개** 들어있습니다.
25단계로 묶여 있고, 사람이 손으로 만든 게 아니라 얼굴 사진 수천 장으로 **미리 학습해 둔 결과**입니다.
우리는 그 파일을 읽어 쓰기만 합니다.

### 어떻게 훑나

작은 사각형 창을 이미지 위에서 한 칸씩 밀면서, 그 안이 얼굴인지 규칙에 통과시킵니다.
한 바퀴 다 돌면 창을 10% 키워서 다시 돕니다. 가까운 얼굴과 먼 얼굴을 모두 잡기 위해서입니다.

640×480 이미지 하나에서 **약 300만 개의 창**을 검사합니다. 그런데도 실시간으로 도는 이유는
**단계별 탈락 구조(cascade)** 때문입니다.

```
1단계 (규칙 9개)   → 탈락하면 즉시 버림
2단계 (규칙 16개)  → 탈락하면 즉시 버림
3단계 (규칙 27개)
...
25단계             → 전부 통과해야 "얼굴"
```

배경 같은 명백한 비얼굴은 1단계에서 걸러집니다. 이미지의 대부분이 배경이므로,
실제로는 규칙 9개만 계산하고 끝나는 창이 압도적으로 많습니다.
전부 계산했다면 88억 회가 필요한 작업이 약 1억 회로 줄어듭니다.

### 코드에서 조정할 수 있는 값

`faceengine.cpp` 의 `detect()` 함수입니다.

```cpp
m_detector.detectMultiScale(small, faces, 1.1, 5, 0, cv::Size(minSide, minSide));
//                                        ↑    ↑              ↑
//                                   scaleFactor  minNeighbors  최소 얼굴 크기
```

| 값 | 의미 | 올리면 | 내리면 |
| --- | --- | --- | --- |
| `1.1` | 창을 키우는 비율 | 빨라짐, 놓칠 수 있음 | 촘촘함, 느려짐 |
| `5` | 같은 자리에서 몇 번 겹쳐 검출돼야 인정 | 확실한 것만, 놓침 증가 | 잘 잡힘, 오검출 증가 |
| `minSide` | 이보다 작은 얼굴은 무시 | 먼 얼굴 무시 | 작은 오검출 증가 |

---

## 4. 검출한 얼굴을 다듬는다 — 정규화

검출 결과는 **위치일 뿐**입니다. 그대로 인식에 넣으면 안 됩니다.

사람마다, 앉은 거리마다 얼굴 크기가 제각각이고 배경도 섞여 있기 때문입니다.
그래서 세 가지 처리를 합니다. (`faceengine.cpp` 의 `cropFace()`)

```cpp
cv::Mat face;
cv::resize(gray(safe), face, cv::Size(200, 200));  // ① 크기 통일
cv::equalizeHist(face, face);                      // ② 밝기 보정
```

**① 얼굴 영역만 잘라냅니다.** 배경이 섞이면 배경까지 그 사람의 특징으로 학습해버립니다.

**② 200×200으로 크기를 통일합니다.** 크기가 다르면 비교 자체가 성립하지 않습니다.

**③ `equalizeHist` 로 밝기를 고르게 폅니다.** 어두운 방에서 찍힌 얼굴과 밝은 곳 얼굴을
비슷한 분포로 만들어 줍니다. 조명 차이에 덜 흔들리게 하는 장치입니다.

> 이 정규화 단계가 인식 정확도의 절반을 좌우합니다. 여기가 어긋나면 뒤가 아무리 좋아도 소용없습니다.

---

## 5. 인식은 어떻게 하나 — LBPH

이름은 **L**ocal **B**inary **P**attern **H**istogram, 우리말로 "국소 이진 패턴 히스토그램" 입니다.
이름 그대로 세 단계입니다.

### ① 국소 이진 패턴 (LBP)

픽셀 하나를 주변 8개 픽셀과 비교합니다. **나보다 밝으면 1, 어두우면 0.**

```
주변 밝기          비교 결과        8자리 이진수
120  135  140      0    1    1
 90  [100] 115  →  0   [ ]  1    →  01101011 = 107
 85   95  110      0    0    1
```

이렇게 하면 모든 픽셀이 0~255 사이 값 하나를 갖게 됩니다.

**핵심은 절대 밝기가 아니라 "이웃보다 밝은가" 만 본다는 점입니다.**
방 전체가 어두워지면 모든 픽셀이 같이 어두워지므로 대소 관계는 그대로입니다.
그래서 조명 변화에 강합니다.

### ② 히스토그램

히스토그램은 **값이 몇 개씩 나왔는지 세어놓은 표**입니다. 어렵지 않습니다.

시험 점수 30개를 10점 단위로 세면 `60점대 3명, 70점대 8명, 80점대 12명...` 이 되는데,
이게 히스토그램입니다. 개별 점수는 사라지고 전체 분포만 남습니다.

LBPH는 이미지를 **격자(8×8 = 64칸)로 나눠 칸마다 따로** LBP 값을 셉니다.

```
64칸 × 256가지 값 = 16,384개의 숫자
```

**왜 칸을 나누냐면**, 전체를 한 번에 세면 위치 정보가 사라지기 때문입니다.
칸별로 세면 "왼쪽 위 칸(눈)에는 이런 패턴, 가운데 칸(코)에는 저런 패턴" 이 남습니다.
이 16,384개 숫자 나열이 그 얼굴의 **지문**입니다.

### ③ 거리 비교

인식할 때는 새 얼굴의 지문과 등록된 지문들을 하나씩 비교해 **가장 가까운 사람**을 고릅니다.
이 "거리" 가 코드에 나오는 `confidence` 이자 화면에 표시되는 **score** 입니다.

> **주의: score는 낮을수록 닮은 것입니다.** 이름이 confidence(신뢰도)라 헷갈리는데,
> 실제로는 거리(distance)라서 0에 가까울수록 동일 인물입니다.

---

## 6. 임계값 — 이 프로젝트에서 가장 중요한 숫자

LBPH는 **무조건 가장 가까운 사람을 고릅니다.** 등록 안 된 사람이 와도 "그나마 제일 비슷한 사람"을
답으로 내놓습니다. 그래서 선을 그어야 합니다.

`faceengine.h`

```cpp
static constexpr double kThreshold = 70.0;   // 이보다 크면 미등록으로 판단
```

```
score  0 ─────── 40 ─────── 70 ─────── 120 ───→
       │  본인    │  애매   │  남/미등록
                          ↑ 임계값
```

### 실측으로 정하는 방법

이 숫자는 환경(조명, 카메라, 등록 사진 품질)마다 달라서 **직접 측정해서 정해야 합니다.**

1. 팀원 전원을 등록한다
2. 각자 인식 시작을 눌러 본인 score 를 기록한다 → 보통 20~50
3. 등록하지 않은 사람이 앉아서 score 를 기록한다 → 보통 80 이상
4. 두 구간 사이에 임계값을 긋는다

본인인데 자꾸 거부되면 올리고, 남이 통과하면 내립니다.
등록 사진의 조명·각도가 다양할수록 두 구간이 잘 벌어져 선 긋기가 쉬워집니다.

### 5프레임 연속 조건

한 프레임만 보고 판단하면 순간적인 오인식으로 남의 계정이 열릴 수 있습니다.
그래서 **같은 사람이 5번 연속 매칭돼야** 인증을 확정합니다. (`recognitionthread.h` 의 `kMatchStreak`)

화면의 `홍길동 (score 42, 3/5)` 에서 `3/5` 가 이 카운트입니다.

---

## 7. 우리 코드 어디를 보면 되나

```
카메라 ─→ CaptureThread ─→ RecognitionThread ─→ FaceEngine
                                    │                ├─ 검출 (Haar)
                                    │                └─ 인식 (LBPH)
                                    ↓
                              MainWindow ─→ DatabaseManager ─→ SQLite
```

| 파일 | 역할 | 여기를 고치면 |
| --- | --- | --- |
| `capturethread.cpp` | 카메라에서 최신 프레임만 계속 읽음 | 카메라 인덱스, 해상도, 백엔드 |
| `faceengine.cpp` | 검출 + 정규화 + 학습 + 인식 | 검출 파라미터, 얼굴 크기, 임계값 |
| `recognitionthread.cpp` | 프레임을 받아 검출/인식을 반복, 결과를 시그널로 전달 | 검출 주기, 샘플 수집 간격, 연속 매칭 횟수 |
| `mainwindow.cpp` | 버튼 연결, 화면 표시, DB 저장, 화면 전환 | 등록 샘플 수, 화면 흐름 |
| `databasemanager.cpp` | 운전자·샘플·인증 이력 저장과 조회 | 스키마, 쿼리 |

### 왜 스레드를 나눴나

카메라를 읽는 작업은 느립니다. UI 스레드에서 하면 **화면이 멈춥니다.**
그래서 캡처 전용 스레드가 뒤에서 계속 돌면서 최신 프레임 한 장만 들고 있고,
표시와 인식은 그걸 가져다 씁니다.

**카메라 한 대당 `CaptureThread` 는 반드시 하나만** 만듭니다.
같은 카메라를 두 번 열면 Windows(DirectShow)에서 두 번째가 실패합니다.
그래서 전방 표시와 얼굴 인식이 같은 캡처 스레드를 공유합니다.

### 왜 인식 스레드가 DB를 직접 안 쓰나

`QSqlDatabase` 커넥션은 **만든 스레드에서만** 사용할 수 있습니다.
그래서 인식 스레드는 검출한 얼굴 이미지를 시그널로 UI 스레드에 넘기고, 실제 저장은 `MainWindow` 가 합니다.

이때 `cv::Mat` 을 시그널 인자로 넘기므로 `main.cpp` 에 이 등록이 반드시 있어야 합니다.

```cpp
qRegisterMetaType<cv::Mat>("cv::Mat");
```

빠뜨리면 시그널이 **조용히 버려져서**, 등록은 성공한 것처럼 보이는데 샘플이 0장으로 남습니다.
(실제로 겪었던 문제입니다)

---

## 8. 데이터 흐름

### 등록할 때

```
[얼굴 등록] 클릭
   → driver 테이블에 이름 INSERT (id 획득)
   → 매 프레임 얼굴 검출 → 200×200으로 잘라냄
   → 검출 3회마다 1장씩 PNG 저장 + face_sample 에 경로 INSERT
   → 20장 모이면 자동 재학습 → lbph_model.yml 저장
```

### 인식할 때

```
[인식 시작] 클릭 → FACE_DETECTION_START 이벤트 저장
   → 매 프레임 얼굴 검출 → 200×200으로 잘라냄
   → LBPH로 가장 가까운 사람 + score 계산
   → score ≤ 70 이고 같은 사람 5회 연속 → 인증 확정
   → auth_log INSERT + FACE_DETECTED 이벤트 저장
   → 잠금 해제 + 계기판 화면으로 전환
```

---

## 9. 잘 안 될 때 — 증상별 원인

| 증상 | 어디가 문제인가 |
| --- | --- |
| 초록 사각형이 안 생김 | **검출 실패.** 조명이 어둡거나, 얼굴이 너무 작거나, 정면이 아님 |
| 등록 진행률이 0에서 안 오름 | 검출 실패 또는 카메라가 안 열림 |
| 본인인데 계속 "미등록" | **임계값 문제.** score 를 보고 `kThreshold` 를 올린다 |
| 남인데 통과됨 | 임계값을 낮춘다. 등록 샘플이 부실해도 이렇게 된다 |
| score 가 항상 비슷하게 나옴 | 등록 샘플이 전부 똑같은 각도·조명일 가능성 |
| "등록된 얼굴이 없습니다" | 샘플이 2장 미만. 운전자 조회에서 샘플 수 확인 |
| 초록 박스 위 글자가 `???` | `cv::putText` 는 한글을 못 그린다. 정상 동작이며 이름은 아래 라벨에 나옴 |

### 검출이 안 될 때 확인 순서

1. 카메라 영상 자체가 나오는가 → 안 나오면 카메라 문제 (인식과 무관)
2. 얼굴이 화면에서 충분히 큰가 → 640×480 기준 얼굴 폭 80px 이상
3. 정면을 보고 있는가 → **우리가 쓰는 cascade는 정면 전용**. 30도 이상 돌리면 못 잡음
4. 조명이 얼굴 정면에서 오는가 → 역광이면 실패

---

## 10. 직접 해보면 이해가 빠릅니다

숫자를 바꿔가며 결과를 보는 게 설명 열 번보다 낫습니다.

**① `minNeighbors` 를 2와 10으로 바꿔보기** (`faceengine.cpp`)
2로 하면 배경을 얼굴로 오검출하는 게 보이고, 10으로 하면 잘 안 잡힙니다.

**② 임계값을 200으로 올려보기** (`faceengine.h`)
아무나 등록된 사람으로 인식됩니다. LBPH가 "무조건 가장 가까운 사람을 고른다" 는 성질이 눈에 보입니다.

**③ 저장된 얼굴 샘플 열어보기**
`faces/1/` 폴더의 PNG 파일들입니다. 실제로 어떤 이미지가 학습에 쓰이는지 보면
"왜 정규화가 중요한가" 가 바로 이해됩니다. 흐릿하거나 얼굴이 잘린 샘플이 섞여 있으면 그게 정확도를 깎습니다.

**④ score 를 기록해보기**
본인, 팀원, 미등록자의 score 를 표로 정리하면 임계값을 어디 둬야 할지 데이터로 보입니다.

---

## 11. 자주 나오는 질문

**Q. 이거 딥러닝인가요?**

아니요. LBPH는 1990년대 방식이고, Haar Cascade는 2001년 방식입니다.
특징을 뽑는 규칙(이웃보다 밝으면 1)을 **사람이 직접 정한** 고전적 컴퓨터 비전 기법입니다.
딥러닝은 그 규칙조차 데이터로부터 스스로 학습합니다.

발표나 문서에 "딥러닝 기반" 이라고 쓰면 틀린 표현입니다. **"LBP 특징 기반 얼굴 인식"** 이 정확합니다.

**Q. LBPH의 "학습" 은 뭔가요?**

등록된 사진마다 히스토그램을 계산해서 저장해두는 게 전부입니다. 반복 최적화 같은 건 없습니다.
학습이라기보다 **암기에 가깝습니다.** 그래서 즉시 끝나고 GPU도 필요 없습니다.

**Q. 왜 각도에 약한가요?**

얼굴을 "눈·코·입이 이렇게 배치된 구조" 로 이해하는 게 아니라 "칸별 명암 패턴 통계" 로만 보기 때문입니다.
고개를 돌리면 패턴이 다른 칸으로 이동해버려서 완전히 다른 지문이 됩니다.
반대로 조명 변화에는 강합니다. 절대 밝기가 아니라 이웃과의 대소 관계만 보니까요.

**Q. 얼굴 사진이 PC에 저장되는 게 부담스러운데요?**

맞습니다. LBPH는 재학습할 때 원본 이미지가 필요해서 `faces/` 폴더에 얼굴 사진이 계속 남습니다.
이걸 없애려면 **임베딩(벡터) 방식**으로 바꿔야 합니다. 얼굴을 128차원 숫자 벡터로 바꿔 DB에 넣고
원본은 즉시 삭제하는 방식인데, 벡터에서 얼굴 복원은 사실상 불가능합니다.

OpenCV의 `FaceRecognizerSF`(SFace)로 구현 가능하지만 ONNX 모델 파일(약 37MB)을 함께 배포해야 합니다.
각도·조명에도 훨씬 강합니다. 지금 규모(4명, 실내 고정)에서는 LBPH로 충분하다고 판단해 이 방식을 택했습니다.

**Q. 왜 20장이나 등록하나요?**

한 장만 등록하면 그 순간의 조명·각도·표정에만 맞춰집니다.
20장을 조금씩 다르게 모으면 "이 사람은 이런 범위 안에서 변한다" 는 폭을 학습하게 됩니다.
그래서 등록할 때 고개를 조금씩 돌리고 표정을 바꾸라고 안내하는 것입니다.

---

## 12. 용어 정리

| 용어 | 뜻 |
| --- | --- |
| 프레임 | 카메라에서 받은 이미지 한 장 |
| 검출 (Detection) | 얼굴이 **어디** 있는지 찾는 것 |
| 인식 (Recognition) | 그 얼굴이 **누구**인지 판별하는 것 |
| Haar Cascade | 명암 대비 규칙을 단계별로 적용하는 검출 기법 |
| LBPH | 국소 명암 패턴의 분포로 사람을 구별하는 인식 기법 |
| 히스토그램 | 값이 몇 개씩 나왔는지 세어놓은 표 |
| 정규화 | 얼굴을 같은 크기·밝기로 맞추는 전처리 |
| score (confidence) | 등록된 얼굴과의 거리. **낮을수록 유사** |
| 임계값 (threshold) | 이 값을 넘으면 미등록으로 판단하는 기준선 |
| 캐스케이드 | 단계별로 걸러내 빠르게 처리하는 구조 |
